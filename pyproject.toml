[tool.poetry]
name = "araviscam"
version = "0.0.3"
description = "Blackfly S GigE camera reader for LVM"
authors = ["Richard J. Mathar"]
license = "BSD-3"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
